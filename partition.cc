Z Q[1001][500];
Z q[100001];

Q[1][1] = 1;
REP1(i, 1, 100000) {
    Q[i%1000][1] = 1;
    q[i] = 1;
    REP1(j, 2, i-1) {
        if (j * (j+1) / 2 > i) break;
        assert(j < 500);
        Q[i%1000][j] = Q[(i-j)%1000][j] + Q[(i-j)%1000][j-1];
        q[i] += Q[i%1000][j];
    }
}